apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - name: httpbin-smoke
    namespace: httpbin-production
    files:
      - smoke.js
    options:
      disableNameSuffixHash: true

resources:
  - job.yaml
